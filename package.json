{
  "name": "joi-to-markdown",
  "version": "1.0.0",
  "description": "Convert Joi schemas to markdown tables",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test mocha --ui bdd --check-leaks --recursive --reporter spec test",
    "test-cov": "NODE_ENV=test istanbul cover --report lcov _mocha -- --ui bdd --check-leaks --recursive --slow 150 --reporter spec test",
    "lint": "eslint ."
  },
  "dependencies": {
    "joi": "^4.8.1",
    "reformat-markdown-table": "^1.1.0"
  },
  "devDependencies": {
    "chai": "^1.10.0",
    "eslint": "^0.9.2",
    "istanbul": "^0.3.2",
    "mocha": "^2.0.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dbrockman/joi-to-markdown.git"
  },
  "bugs": {
    "url": "https://github.com/dbrockman/joi-to-markdown/issues"
  },
  "homepage": "https://github.com/dbrockman/joi-to-markdown",
  "author": "David Brockman Smoliansky",
  "license": "MIT"
}
